# This is a public project: resolve from the public registry, whatever the
# machine's global npm configuration says. Without this, a lockfile written on a
# machine behind a private mirror records that mirror's URLs, which then leak
# into the repository and break CI.
registry=https://registry.npmjs.org/
