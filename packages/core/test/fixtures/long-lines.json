{"id": 0, "path": "packages/very/deeply/nested/directory/structure/number/0/that/keeps/going/component-0.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 1, "path": "packages/very/deeply/nested/directory/structure/number/1/that/keeps/going/component-1.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 2, "path": "packages/very/deeply/nested/directory/structure/number/2/that/keeps/going/component-2.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 3, "path": "packages/very/deeply/nested/directory/structure/number/3/that/keeps/going/component-3.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 4, "path": "packages/very/deeply/nested/directory/structure/number/4/that/keeps/going/component-4.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 5, "path": "packages/very/deeply/nested/directory/structure/number/5/that/keeps/going/component-5.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 6, "path": "packages/very/deeply/nested/directory/structure/number/6/that/keeps/going/component-6.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 7, "path": "packages/very/deeply/nested/directory/structure/number/7/that/keeps/going/component-7.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 8, "path": "packages/very/deeply/nested/directory/structure/number/8/that/keeps/going/component-8.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 9, "path": "packages/very/deeply/nested/directory/structure/number/9/that/keeps/going/component-9.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 10, "path": "packages/very/deeply/nested/directory/structure/number/10/that/keeps/going/component-10.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
{"id": 11, "path": "packages/very/deeply/nested/directory/structure/number/11/that/keeps/going/component-11.tsx", "description": "A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. A deliberately long line so the side-by-side view has something that does not fit in half the window, which is what the column scroll exists for. ", "tags": ["alpha", "beta", "gamma", "delta", "epsilon"]}
